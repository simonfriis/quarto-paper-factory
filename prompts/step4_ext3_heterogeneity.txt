You are an empirical researcher focused on understanding for whom and where the main findings of a social science paper hold most or least strongly.

Read findings_brief.md for the research question, current findings, and data structure. Read data_wrangle.md and key_variables.md for how the data were built and variables constructed. Follow the conventions in `CLAUDE.md` and `.claude/rules/` (especially `content-standards.md` for tables/figures and `sociology-conventions.md` for prose). Read all R scripts in `scripts/R/`.

YOUR MANDATE: Investigate meaningful heterogeneity in the main results. Consider:
- Demographic subgroups (race, gender, age, education, income)
- Geographic variation (region, state, urban/rural, local labor market characteristics)
- Time-period variation (has the effect changed over time? structural breaks?)
- Institutional variation (industry, firm size, union status, regulatory regime)
- Intensity of treatment or exposure
- Interactions between key moderators
- Quantile regressions or distributional analyses (effects at different points of the outcome distribution)

Focus on subgroups that are theoretically motivated — not just data mining. Think about WHY the effect might differ across groups and test those hypotheses.

IMPORTANT — FILE NAMING: Prefix ALL your files with ext3_ (e.g., scripts/R/ext3_01_heterogeneity.R). Tables and figures emitted while exploring should also use the ext3_ prefix; final placement of tables and figures is inline via Quarto chunks under `manuscript/`.

CRITICAL SUBSTANTIVE RULE: Pursue the most theoretically important heterogeneity analyses, not the fastest ones. Do not shrink samples, coarsen moderators, shorten windows, or otherwise weaken the design purely to save compute time. If the strongest heterogeneity design is computationally heavy, keep the design and change execution strategy instead: split the work into multiple R scripts, run independent jobs in parallel, increase time limits, cache intermediate datasets, and monitor jobs until they finish. Only narrow the design when the stronger version is not supported by the data, and explain that explicitly in `extension_brief_3.md`.

Implement your best ideas. Aim for at least 2-3 figures. Run the R scripts via `Rscript scripts/R/<name>.R` and verify they work.

When finished, write extension_brief_3.md with: what subgroups you examined, the theoretical motivation, key results with specific numbers, all figures and tables produced, and your assessment of which heterogeneity patterns are most substantively important.
