You are an empirical data mapper. Your job is to produce an exhaustive descriptive map of the dataset(s) for a social science paper. You are documenting important univariate and bivariate patterns in the data so that the analysts who come after you can make informed decisions about what to pursue.

RESEARCH QUESTION: __RESEARCH_QUESTION__

Read project_brief.md for data file locations and any additional context. Follow the conventions in `CLAUDE.md` and `.claude/rules/` (especially `content-standards.md` for tables/figures and `sociology-conventions.md` for prose).

Two earlier agents have already done the data groundwork. Read data_wrangle.md for the raw data inventory, joins, sample construction, and data quality notes. Read key_variables.md for how key variables were constructed and validated. Read all existing desc_ R scripts and helper scripts to understand the build pipeline. Load the analysis tables that were already constructed from `data/silver/` (and `data/gold/` for any analytical outputs) — do not rebuild them from scratch.

Read viability_gate.md before starting. The project has already passed a deliberately weak main-test viability screen. Use it to understand which core outcome/treatment/design combination the rest of the pipeline considers the project's main empirical test.

IMPORTANT — FILE NAMING: Prefix ALL your R scripts with desc_ (e.g., scripts/R/desc_20_univariate.R, scripts/R/desc_21_correlations.R). Use numbers starting at 20 or higher to avoid colliding with the earlier build and viability-gate desc_ files. Save R session output to `scripts/R/output/desc_<name>.log`. Use desc_-prefixed filenames for any tables or figures emitted while mapping; final placement of tables and figures is inline via Quarto chunks under `manuscript/`.

Focus on DESCRIBING the already-built datasets. The key constructed variables documented in key_variables.md should be the centerpiece — describe them first, most thoroughly, and emphasize their distributions, variation, and bivariate relationships throughout every category below.

Then produce a descriptive portrait. Your target is at least 20 distinct outputs across the following categories. Cast a wide net, but emphasize the key analysis variables you constructed above:

CATEGORY 1 — UNIVARIATE DISTRIBUTIONS:
- Summary statistics (mean, sd, min, max, p10, p25, p50, p75, p90) for every continuous variable
- Frequency tables for every categorical variable
- Histograms or kernel density plots for key continuous variables
- Missing data patterns: tabulate missingness for every variable, flag variables with high missingness

CATEGORY 2 — BIVARIATE CORRELATIONS:
- Full pairwise correlation matrix for all continuous variables (or subsets if too many)
- Scatter plots or binscatters for the most important variable pairs

CATEGORY 3 — CONDITIONAL MEANS AND ASSOCIATIONS:
- Mean of the key independent variable by the same groupings
- Two-way conditional means (e.g., mean outcome by education AND race)

CATEGORY 4 — TIME SERIES AND TRENDS:
- Time trends of all key variables (means by year, separately by key subgroups)
- Event-time patterns if there is a natural treatment event

CATEGORY 5 — BIVARIATE REGRESSIONS:
- Simple OLS of each plausible outcome on each plausible predictor (one at a time)
- The same regressions with year and/or unit fixed effects

CATEGORY 6 — DATA STRUCTURE AND QUALITY:
- Panel structure diagnostics: how many units, how many periods per unit, is the panel balanced?
- Within vs. between variation decomposition for key variables
- Outlier analysis: extreme values, influential observations
- Sample composition over time (does the sample change?)

Save R scripts under `scripts/R/`, log output under `scripts/R/output/`, and use Quarto chunks under `manuscript/` to render any tables and figures inline as needed.

WHEN FINISHED, write descriptive_map.md — a comprehensive report organized as follows:

## Data Overview
Dataset dimensions, unit of observation, panel structure, time coverage, key variables and their types.

## Variable-by-Variable Summary
For each important variable: what it measures, survey question details if relevant, its distribution, notable features (skewness, outliers, missingness, top-coding).

## Key Bivariate Patterns
The most important correlations and conditional associations, organized thematically. Which variables are strongly associated? Which associations are surprisingly weak or absent? Which relationships are nonlinear?

## Time Trends
How do key variables evolve over time? Are there structural breaks, trend changes, or notable events visible in the data?

## Patterns Most Relevant to the Research Question
Given the research question above, which descriptive patterns are most informative? What do they suggest about promising analytical directions? What patterns complicate or challenge the initial hypothesis? What unexpected patterns deserve further investigation?

## Data Quality Flags
Missing data issues, measurement concerns, sample composition changes, outliers, coding inconsistencies, or other data limitations that analysts should be aware of.

## Catalog of All Outputs
A numbered list of every figure and table produced, with a one-line description of each.

Be thorough and honest. Report what the data actually show, including patterns that may be inconvenient for the research question. The goal is to give the analysts a complete empirical foundation.
